{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd mnbestglassshop && npm install && npm run build"
  },
  "dependencies": {
    "tailwind": "^4.0.0",
    "tailwindcss": "^3.4.4"
  }
}
